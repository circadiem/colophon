"""The rule build. Stamped on every result and every assertion channels/ produces (CLAUDE.md I6).

Bump on any change to the arithmetic, the branch conditions, the status boundaries or the reason
codes. A bump produces new assertions that supersede the old ones (I4); see replay.py.
"""

RULE_VERSION = "0.2.0"
