"""Known cases for the channel engine, as data.

Each fixture quotes its governing text verbatim (docs/II §05 or the Step 0 answers) and states the
expected outputs. The tests in tests/test_*.py assert against these; nothing here computes.
"""
